# v2.3.1 系列版本规划 - BaseAdapter 请求处理拆分

> **文档编号**: PLAN-V2.3.1-SERIES
> **创建日期**: 2026 年 4 月 22 日
> **作者**: AI Assistant
> **版本**: v1.0
> **状态**: 待执行

---

## 📋 目录

1. [v2.3.1 系列概述](#v231-系列概述)
2. [子版本列表](#子版本列表)
3. [v2.3.1.1 详细规划](#v2311-详细规划)
4. [v2.3.1.2 详细规划](#v2312-详细规划)
5. [v2.3.1.3 详细规划](#v2313-详细规划)
6. [v2.3.1.4 详细规划](#v2314-详细规划)
7. [Git 提交策略](#git-提交策略)
8. [验收标准](#验收标准)

---

## 📊 v2.3.1 系列概述

### 系列目标

将 BaseAdapter 中的复杂请求处理逻辑（约 300 行）拆分为独立组件：
- RequestProcessor：请求处理核心逻辑
- ResponseMapper：响应数据转换和映射

### 版本拆分策略

采用**渐进式拆分**策略，每个子版本聚焦一个独立功能：

| 子版本 | 主题 | 工作量 | Git 标签 |
|--------|------|--------|---------|
| v2.3.1.1 | 创建 RequestProcessor 组件 | 0.5 天 | v2.3.1.1 |
| v2.3.1.2 | 创建 ResponseMapper 组件 | 0.5 天 | v2.3.1.2 |
| v2.3.1.3 | 重构 BaseAdapter 非流式请求 | 0.5 天 | v2.3.1.3 |
| v2.3.1.4 | 重构 BaseAdapter 流式请求 + 测试 | 0.5 天 | v2.3.1.4 |

**总计**: 4 个子版本，2 天工作量

---

## 📝 v2.3.1.1 详细规划

### 版本信息

| 属性 | 详情 |
|------|------|
| **版本** | v2.3.1.1 |
| **主题** | 创建 RequestProcessor 组件 |
| **工作量** | 0.5 天 (4 小时) |
| **Git 标签** | v2.3.1.1 |

### 目标

创建 RequestProcessor 组件，处理请求构建和发送逻辑

### 新增文件

**RequestProcessor.java** (~150 行)
- 位置：`adapter/processor/RequestProcessor.java`
- 职责：请求构建、发送、超时处理

### 核心方法

```java
public class RequestProcessor {
    
    /**
     * 发送非流式请求
     */
    public <T> Mono<ClientResponse> sendNonStreaming(
        T request,
        String url,
        Map<String, String> headers,
        Duration timeout
    );
    
    /**
     * 发送流式请求
     */
    public <T> Flux<DataBuffer> sendStreaming(
        T request,
        String url,
        Map<String, String> headers
    );
    
    /**
     * 配置请求头
     */
    public WebClient.RequestBodySpec configureHeaders(
        WebClient.RequestBodySpec spec,
        Map<String, String> headers
    );
}
```

### 实施步骤

1. 创建 RequestProcessor 类 (1 小时)
2. 实现 sendNonStreaming 方法 (1 小时)
3. 实现 sendStreaming 方法 (1 小时)
4. 编写单元测试 (1 小时)

### 验收标准

- [ ] RequestProcessor 编译通过
- [ ] 单元测试覆盖率 > 80%
- [ ] 10+ 个测试方法
- [ ] Git 提交并打标签 v2.3.1.1

### Git 提交

```bash
git commit -m "feat(v2.3.1.1): 创建 RequestProcessor 组件

新增组件:
- RequestProcessor (~150 行): 请求处理核心逻辑

核心方法:
- sendNonStreaming(): 发送非流式请求
- sendStreaming(): 发送流式请求
- configureHeaders(): 配置请求头

测试:
- RequestProcessorTest (10+ 个测试)

# v2.3.1.1 请求处理拆分 - 阶段 1"

git tag v2.3.1.1 -m "v2.3.1.1: 创建 RequestProcessor 组件

新增文件：1 个
新增代码：~150 行
测试方法：10+ 个"
```

---

## 📝 v2.3.1.2 详细规划

### 版本信息

| 属性 | 详情 |
|------|------|
| **版本** | v2.3.1.2 |
| **主题** | 创建 ResponseMapper 组件 |
| **工作量** | 0.5 天 (4 小时) |
| **Git 标签** | v2.3.1.2 |

### 目标

创建 ResponseMapper 组件，处理响应数据转换和映射

### 新增文件

**ResponseMapper.java** (~120 行)
- 位置：`adapter/mapper/ResponseMapper.java`
- 职责：响应映射、错误处理、JSON 转换

### 核心方法

```java
public class ResponseMapper {
    
    /**
     * 映射非流式响应
     */
    public <T> Mono<ResponseEntity<T>> mapResponse(
        ClientResponse clientResponse,
        Class<T> targetType
    );
    
    /**
     * 映射流式响应块
     */
    public String mapStreamChunk(DataBuffer dataBuffer);
    
    /**
     * 处理响应错误
     */
    public Mono<Throwable> handleResponseError(
        ClientResponse clientResponse,
        String instanceName
    );
    
    /**
     * JSON 转换
     */
    public <T> T fromJson(String json, Class<T> targetType);
    public String toJson(Object object);
}
```

### 实施步骤

1. 创建 ResponseMapper 类 (1 小时)
2. 实现 mapResponse 方法 (1 小时)
3. 实现 mapStreamChunk 和错误处理 (1 小时)
4. 编写单元测试 (1 小时)

### 验收标准

- [ ] ResponseMapper 编译通过
- [ ] 单元测试覆盖率 > 80%
- [ ] 8+ 个测试方法
- [ ] Git 提交并打标签 v2.3.1.2

### Git 提交

```bash
git commit -m "feat(v2.3.1.2): 创建 ResponseMapper 组件

新增组件:
- ResponseMapper (~120 行): 响应数据转换和映射

核心方法:
- mapResponse(): 映射非流式响应
- mapStreamChunk(): 映射流式响应块
- handleResponseError(): 处理响应错误
- fromJson()/toJson(): JSON 转换

测试:
- ResponseMapperTest (8+ 个测试)

# v2.3.1.2 请求处理拆分 - 阶段 2"

git tag v2.3.1.2 -m "v2.3.1.2: 创建 ResponseMapper 组件

新增文件：1 个
新增代码：~120 行
测试方法：8+ 个"
```

---

## 📝 v2.3.1.3 详细规划

### 版本信息

| 属性 | 详情 |
|------|------|
| **版本** | v2.3.1.3 |
| **主题** | 重构 BaseAdapter 非流式请求 |
| **工作量** | 0.5 天 (4 小时) |
| **Git 标签** | v2.3.1.3 |

### 目标

重构 BaseAdapter 的非流式请求处理方法，委托给新组件

### 修改文件

| 文件 | 变更内容 |
|------|---------|
| BaseAdapter.java | 注入 RequestProcessor 和 ResponseMapper |
| BaseAdapter.java | 重构 processRequest 方法 (300 行→50 行) |
| 6 个 Adapter 实现 | 无需修改（构造函数不变） |

### 重构前

```java
// BaseAdapter.processRequest() - 约 300 行
protected <T> Mono processRequest(...) {
    // 1. 请求转换 (50 行)
    // 2. 构建请求 (80 行)
    // 3. 发送请求 (60 行)
    // 4. 处理响应 (80 行)
    // 5. 错误处理 (30 行)
}
```

### 重构后

```java
// BaseAdapter.processRequest() - 约 50 行
protected <T> Mono processRequest(...) {
    // 1. 准备参数
    // 2. 委托给 RequestProcessor.sendNonStreaming()
    // 3. 委托给 ResponseMapper.mapResponse()
    // 4. 错误处理委托给 AdapterErrorHandler
}
```

### 实施步骤

1. 在 BaseAdapter 中注入新组件 (0.5 小时)
2. 重构 processRequest 方法 (2 小时)
3. 运行现有测试验证 (1.5 小时)

### 验收标准

- [ ] BaseAdapter 编译通过
- [ ] processRequest 方法行数 < 50 行
- [ ] 所有现有测试通过
- [ ] Git 提交并打标签 v2.3.1.3

### Git 提交

```bash
git commit -m "feat(v2.3.1.3): 重构 BaseAdapter 非流式请求

重构内容:
- 注入 RequestProcessor 和 ResponseMapper
- processRequest 方法委托 (300 行→50 行，-83%)
- 错误处理委托给 AdapterErrorHandler

影响范围:
- BaseAdapter: 重构 processRequest 方法
- 现有测试：全部通过

代码统计:
- 修改文件：1 个
- 删除代码：~250 行

# v2.3.1.3 请求处理拆分 - 阶段 3"

git tag v2.3.1.3 -m "v2.3.1.3: 重构 BaseAdapter 非流式请求

修改文件：1 个
删除代码：~250 行
方法行数：300→50 (-83%)"
```

---

## 📝 v2.3.1.4 详细规划

### 版本信息

| 属性 | 详情 |
|------|------|
| **版本** | v2.3.1.4 |
| **主题** | 重构 BaseAdapter 流式请求 + 测试 |
| **工作量** | 0.5 天 (4 小时) |
| **Git 标签** | v2.3.1.4 |

### 目标

重构 BaseAdapter 的流式请求处理方法，并编写完整测试

### 修改文件

| 文件 | 变更内容 |
|------|---------|
| BaseAdapter.java | 重构 processStreamingRequest 方法 (200 行→30 行) |
| RequestProcessorTest.java | 新增测试 |
| ResponseMapperTest.java | 新增测试 |
| BaseAdapterRequestIntegrationTest.java | 新增集成测试 |

### 重构前

```java
// BaseAdapter.processStreamingRequest() - 约 200 行
protected Mono processStreamingRequest(...) {
    // 1. 构建流式请求 (60 行)
    // 2. 发送流式请求 (50 行)
    // 3. 处理 SSE 流 (60 行)
    // 4. 错误处理 (30 行)
}
```

### 重构后

```java
// BaseAdapter.processStreamingRequest() - 约 30 行
protected Mono processStreamingRequest(...) {
    // 1. 准备参数
    // 2. 委托给 RequestProcessor.sendStreaming()
    // 3. 委托给 ResponseMapper.mapStreamChunk()
    // 4. 错误处理委托给 AdapterErrorHandler
}
```

### 新增测试

**RequestProcessorTest** (新增 10 个测试)
- testSendNonStreaming_Success
- testSendNonStreaming_Error
- testSendStreaming_Success
- testSendStreaming_Error
- testConfigureHeaders

**ResponseMapperTest** (新增 8 个测试)
- testMapResponse_Success
- testMapResponse_Error
- testMapStreamChunk
- testHandleResponseError
- testFromJson
- testToJson

**BaseAdapterRequestIntegrationTest** (15 个测试)
- testProcessRequest_NonStreaming
- testProcessRequest_Streaming
- testProcessRequest_Error
- testProcessRequest_Retry

### 实施步骤

1. 重构 processStreamingRequest 方法 (1.5 小时)
2. 编写 RequestProcessorTest (1 小时)
3. 编写 ResponseMapperTest (1 小时)
4. 编写集成测试 (0.5 小时)

### 验收标准

- [ ] BaseAdapter 编译通过
- [ ] processStreamingRequest 方法行数 < 30 行
- [ ] 新增测试 33+ 个
- [ ] 所有测试通过
- [ ] Git 提交并打标签 v2.3.1.4

### Git 提交

```bash
git commit -m "feat(v2.3.1.4): 重构 BaseAdapter 流式请求 + 测试

重构内容:
- processStreamingRequest 方法委托 (200 行→30 行，-85%)
- 流式处理委托给 RequestProcessor 和 ResponseMapper

新增测试:
- RequestProcessorTest (10 个测试)
- ResponseMapperTest (8 个测试)
- BaseAdapterRequestIntegrationTest (15 个测试)

代码统计:
- 修改文件：1 个
- 新增文件：3 个 (测试)
- 删除代码：~170 行
- 新增测试：33+ 个

# v2.3.1.4 请求处理拆分 - 阶段 4"

git tag v2.3.1.4 -m "v2.3.1.4: 重构 BaseAdapter 流式请求 + 测试

修改文件：1 个
新增文件：3 个 (测试)
删除代码：~170 行
测试方法：33+ 个"
```

---

## 📦 Git 提交策略

### 分支策略

```
master
  │
  ├── v2.3.1.1 开发完成 → 本地提交 → 本地标签 v2.3.1.1
  │
  ├── v2.3.1.2 开发完成 → 本地提交 → 本地标签 v2.3.1.2
  │
  ├── v2.3.1.3 开发完成 → 本地提交 → 本地标签 v2.3.1.3
  │
  └── v2.3.1.4 开发完成 → 本地提交 → 本地标签 v2.3.1.4
```

### 提交历史示例

```
* v2.3.1.4 (tag: v2.3.1.4) - 重构 BaseAdapter 流式请求 + 测试
* v2.3.1.3 (tag: v2.3.1.3) - 重构 BaseAdapter 非流式请求
* v2.3.1.2 (tag: v2.3.1.2) - 创建 ResponseMapper 组件
* v2.3.1.1 (tag: v2.3.1.1) - 创建 RequestProcessor 组件
* v2.3.0-final (tag: v2.3.0-final) - v2.3.0 完成
```

### 最终推送

v2.3.1 系列完成后，统一推送到远程仓库：

```bash
git push origin master --tags
```

---

## ✅ 验收标准

### 代码质量

- [ ] 所有新组件单元测试覆盖率 > 80%
- [ ] Checkstyle 0 违规
- [ ] SpotBugs 0 警告
- [ ] 所有测试通过

### 架构改进

- [ ] BaseAdapter processRequest 方法 < 50 行
- [ ] BaseAdapter processStreamingRequest 方法 < 30 行
- [ ] RequestProcessor 职责单一
- [ ] ResponseMapper 职责单一

### 功能完整性

- [ ] 非流式请求功能正常
- [ ] 流式请求功能正常
- [ ] 错误处理正常
- [ ] 性能无明显下降

---

**文档版本**: v1.0
**创建日期**: 2026-04-22
**下一步**: 开始 v2.3.1.1 实施
